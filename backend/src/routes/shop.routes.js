const r=require("express").Router(); const c=require("../controllers/shop.controller"); const auth=require("../middleware/auth"); const role=require("../middleware/role"); r.get("/",c.publicList); r.get("/public/:slug",c.getPublic); r.post("/",auth,role("shop_owner"),c.create); r.get("/mine",auth,role("shop_owner"),c.mine); r.patch("/:id/live",auth,role("shop_owner"),c.setLiveStatus); r.patch("/:id",auth,role("shop_owner"),c.update); module.exports=r;