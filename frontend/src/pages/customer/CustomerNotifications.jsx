import React,{useEffect,useState} from 'react';import api from '../../api/client';import './CustomerNotifications.css';export default function CustomerNotifications(){const[list,setList]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState('');const load=()=>{setLoading(true);api.get('/customer/notifications').then(r=>setList(Array.isArray(r.data?.notifications)?r.data.notifications:[])).catch(e=>setError(e.response?.data?.message||'Unable to load notifications')).finally(()=>setLoading(false))};useEffect(()=>{load();},[]);if(loading)return <section className="portal-card">Loading notifications…</section>;if(error)return <section className="portal-card"><h2>Notifications unavailable</h2><p>{error}</p><button onClick={load}>Retry</button></section>;return <><div className="portal-heading"><div><span>Updates</span><h1>Notifications</h1><p>Order, bill, offer and account alerts.</p></div></div><section className="portal-card notification-list">{list.map(n=><article className={n.isRead?'':'unread'} key={n._id} onClick={()=>api.patch(`/customer/notifications/${n._id}/read`).then(load)}><div><b>{n.title}</b><p>{n.message}</p><small>{new Date(n.createdAt).toLocaleString()}</small></div>{!n.isRead&&<span>New</span>}</article>)}{!list.length&&<p className="muted">No notifications.</p>}</section></>};
