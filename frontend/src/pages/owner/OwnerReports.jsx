import React,{useEffect,useState}from"react";import api from"../../api/client";import"./OwnerReports.css";const money=n=>`₹${((n||0)/100).toLocaleString("en-IN")}`;export default function OwnerReports(){const[data,setData]=useState({sales:[],paymentMethods:{},fulfilment:{}}),[error,setError]=useState("");useEffect(()=>{api.get("/shops/mine").then(r=>{const s=r.data?.shops?.[0];return s?api.get(`/owner/${s._id}/reports`):null}).then(r=>r&&setData(r.data)).catch(e=>setError(e.response?.data?.message||"Unable to load reports"))},[]);const max=Math.max(1,...data.sales.map(x=>x.totalPaise));if(error)return <section className="report-panel"><h2>Reports unavailable</h2><p>{error}</p></section>;return <><div className="owner-page-head"><div><span>Insights</span><h1>Sales reports</h1><p>Understand sales movement, payments and fulfilment.</p></div></div><section className="report-panel"><h2>Daily sales</h2><div className="bars">{data.sales.slice(-14).map(x=><div className="bar-item" key={x.date}><span style={{height:`${Math.max(8,x.totalPaise/max*180)}px`}}></span><small>{x.date.slice(5)}</small><b>{money(x.totalPaise)}</b></div>)}</div>{!data.sales.length&&<p>No paid sales yet.</p>}</section><div className="report-grid"><section className="report-panel"><h2>Payment methods</h2>{Object.entries(data.paymentMethods).map(([k,v])=><div className="metric-row" key={k}><span>{k}</span><b>{v}</b></div>)}</section><section className="report-panel"><h2>Fulfilment</h2>{Object.entries(data.fulfilment).map(([k,v])=><div className="metric-row" key={k}><span>{k.replaceAll('_',' ')}</span><b>{v}</b></div>)}</section></div></>};
