import React from'react';import{useWorkspace}from'../../context/WorkspaceContext';import{Link}from'react-router-dom';import'./OwnerShops.css';
export default function OwnerShops(){const{workspaces,activeShop,setActiveId}=useWorkspace();return <div><div className="dash-title"><div><h1>My shops</h1><p>Switch between stores while keeping inventory, staff and reports isolated.</p></div></div><div className="shop-workspace-grid">{workspaces.filter(w=>w.role==='owner').map(w=><article className={activeShop?._id===w.shop._id?'active':''} key={w.shop._id}>{w.shop.logoUrl?<img src={`${import.meta.env.VITE_API_ORIGIN||'http://localhost:5000'}${w.shop.logoUrl}`} alt=""/>:<div className="shop-logo-placeholder">{w.shop.name[0]}</div>}<div><h3>{w.shop.name}</h3><p>{w.shop.category} · {w.shop.address?.city||'Location pending'}</p><span className={`status-pill ${w.shop.status}`}>{w.shop.status}</span></div><button onClick={()=>setActiveId(w.shop._id)}>{activeShop?._id===w.shop._id?'Active shop':'Switch to shop'}</button><Link to="/owner/settings" onClick={()=>setActiveId(w.shop._id)}>Edit shop</Link></article>)}</div></div>}
