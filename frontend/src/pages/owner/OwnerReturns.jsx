import React,{useEffect,useState}from'react';import api from'../../api/client';import useOwnerShop from'./useOwnerShop';import'./OwnerOperations.css';
export default function OwnerReturns(){const{shop}=useOwnerShop();const[list,setList]=useState([]);const load=()=>shop&&api.get(`/operations/${shop._id}/returns`).then(r=>setList(r.data.returns||[]));useEffect(()=>{load()},[shop]);const update=async(id,status)=>{await api.patch(`/operations/returns/${id}`,{status,restoreStock:status==='received'});load()};return <div><div className="dash-title"><div><h1>Returns, exchanges & refunds</h1><p>Review requests, receive returned stock and track refund status.</p></div></div><div className="ops-table"><table><thead><tr><th>Order</th><th>Customer</th><th>Type</th><th>Reason</th><th>Status</th><th>Action</th></tr></thead><tbody>{list.map(r=><tr key={r._id}><td>{r.orderId?.orderNumber}</td><td>{r.customerId?.name||'Guest'}</td><td>{r.type}</td><td>{r.reason}</td><td>{r.status}</td><td><select value={r.status} onChange={e=>update(r._id,e.target.value)}>{['requested','approved','rejected','received','refunded','exchanged','closed'].map(x=><option key={x}>{x}</option>)}</select></td></tr>)}</tbody></table>{!list.length&&<p className="empty-ops">No return requests yet.</p>}</div></div>}
