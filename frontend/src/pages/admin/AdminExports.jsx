import React from "react";import api from "../../api/client";import "./admin.css";export default function AdminExports(){const download=async type=>{const r=await api.get(`/governance/exports/${type}`,{responseType:'blob'});const url=URL.createObjectURL(r.data);const a=document.createElement('a');a.href=url;a.download=`pocketstore-${type}.csv`;a.click();URL.revokeObjectURL(url)};return <><div className="admin-title"><div><h1>Data exports</h1><p>Download operational datasets for authorised analysis and backup.</p></div></div><div className="export-grid">{[['users','Users'],['shops','Shops'],['orders','Orders']].map(([k,l])=><section className="admin-panel export-card" key={k}><h2>{l}</h2><p>Export the current {l.toLowerCase()} dataset as CSV.</p><button className="primary-action" onClick={()=>download(k)}>Download CSV</button></section>)}</div></>}
