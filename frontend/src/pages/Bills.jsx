import {useEffect,useState} from "react"; import api from "../api/client"; export default function Bills(){const [items,setItems]=useState([]);useEffect(()=>{api.get("/invoices/mine").then(r=>setItems(r.data.invoices))},[]);return <main className="page"><h1>Digital bills</h1><div className="list">{items.map(i=><article key={i._id}><div><b>{i.invoiceNumber}</b><p>{i.shopId?.name} · {new Date(i.issuedAt).toLocaleDateString()}</p></div><div><b>₹{(i.totalPaise/100).toFixed(2)}</b><a href={`/bill/${i.publicToken}`}>View</a></div></article>)}</div></main>}
