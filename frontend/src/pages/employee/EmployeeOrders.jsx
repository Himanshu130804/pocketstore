import React,{useEffect,useState}from'react';import api from'../../api/client';import{useWorkspace}from'../../context/WorkspaceContext';import DataTable from'../../components/common/DataTable';import'./employee.css';
export default function EmployeeOrders(){const{activeShop,role}=useWorkspace();const[orders,setOrders]=useState([]);const load=()=>activeShop&&api.get(`/workspace/${activeShop._id}/orders`).then(r=>setOrders(r.data.orders||[]));useEffect(()=>{load()?.catch(()=>{})},[activeShop?._id]);const update=async(o,value)=>{const body=role==='delivery_staff'?{deliveryStatus:value}:{status:value};await api.patch(`/workspace/orders/${o._id}`,body);load()};const columns=[{key:'orderNumber',label:'Order'},{key:'customer',label:'Customer',render:o=>o.customerContact?.name||o.customerContact?.phone||'Guest'},{key:'mode',label:'Mode',render:o=>o.fulfilmentMode.replaceAll('_',' ')},{key:'total',label:'Total',render:o=>`₹${((o.totalPaise||0)/100).toFixed(2)}`},{key:'status',label:'Status',render:o=><span className={`status-pill ${role==='delivery_staff'?o.deliveryStatus:o.status}`}>{(role==='delivery_staff'?o.deliveryStatus:o.status)?.replaceAll('_',' ')}</span>},{key:'action',label:'Update',render:o=>role==='delivery_staff'?<select value={o.deliveryStatus} onChange={e=>update(o,e.target.value)}><option value="assigned">Assigned</option><option value="picked_up">Picked up</option><option value="on_the_way">On the way</option><option value="delivered">Delivered</option><option value="failed">Failed</option></select>:<select value={o.status} onChange={e=>update(o,e.target.value)}><option value="placed">Placed</option><option value="accepted">Accepted</option><option value="preparing">Preparing</option><option value="ready">Ready</option><option value="completed">Completed</option></select>}];return <><div className="dash-title"><div><h1>{role==='delivery_staff'?'My deliveries':'Shop orders'}</h1><p>Only actions permitted for your role are available.</p></div></div><DataTable columns={columns} rows={orders} empty="No assigned orders"/></>}
